<!-- 裁判身份：比赛通知 -->
<view class="block-container" wx:if="{{isReferee}}">
  <view class="block-top" bindtap="toggleCoachInvitationInform">
    <view class="block-title">
      <text>裁判通知中心</text>
    </view>
  </view>
  <view class="block-main">
    <view class="hint-text">与您执法的球队及其比赛相关的通知将在这里展示。</view>
  </view>
</view>

<view wx:for="{{notifications}}" wx:key="key">
  <notification-block
    visible="{{item.visible}}"
    title="{{item.title}}"
    list="{{item.list}}"
    open="{{item.open}}"
    hint="{{item.hint}}"
    emptyText="{{item.emptyText}}"
    data-index="{{index}}"
    data-event="{{item.event}}"
    bind:toggle="toggleNotification"
    bind:itemclick="handleNotificationClick"
  />
</view>

<!-- <view class="block-container" wx:if="{{isReferee}}">
  <view class="block-top" bindtap="toggleRefereeMatchInform">
    <view class="block-title">
      <text>比赛通知</text>
      <view class="red-dot" wx:if="{{showRefereeMatchDot}}"></view>
    </view>
    <view class="block-more">
      <image src="/assets/down333.png" mode="" class="{{showRefereeMatchInform ? 'icon-rotated' : 'icon'}}"/>
    </view>
  </view>
  <view class="block-main">
    <view class="hint-text">您两周内的比赛</view>
    <scroll-view scroll-y="true" style="height: {{showRefereeMatchInform ? '200px' : '0'}}; visibility: {{showRefereeMatchInform ? 'visible' : 'hidden'}};" class="notification-box">
      <view class="invitation-item" wx:if="{{refereeMatchInform.length==0}}">
        <text class="invitation-text">您近两星期内没有比赛</text>
      </view>
      <view class="invitation-item" wx:for="{{refereeMatchInform}}" wx:key="index">
        <text class="invitation-text">{{item}}</text>
      </view>
    </scroll-view>
  </view>
</view>

<view class="block-container" wx:if="{{isReferee}}">
  <view class="block-top" bindtap="toggleRefereeInvitationInformForMatch">
    <view class="block-title">
      <text>比赛邀请通知</text>
      <view class="red-dot" wx:if="{{showRefereeInvitationDotForMatch}}"></view>
    </view>
    <view class="block-more">
      <image src="/assets/down333.png" mode="" class="{{showRefereeInvitationInformForMatch ? 'icon-rotated' : 'icon'}}"/>
    </view>
  </view>
  <view class="block-main">
    <view class="hint-text">点击邀请可选择接受或拒绝</view>
    <scroll-view scroll-y="true" style="height: {{showRefereeInvitationInformForMatch ? '200px' : '0'}}; visibility: {{showRefereeInvitationInformForMatch ? 'visible' : 'hidden'}};" class="notification-box">
      <view class="invitation-item" wx:if="{{refereeInvitationInformForMatch.length==0}}">
        <text class="invitation-text">您还没有收到任何比赛发出的邀请。</text>
      </view>
      <view class="invitation-item" wx:for="{{refereeInvitationInformForMatch}}" wx:key="index" bind:tap="showRefereeMatchInvitationModal" data-id="{{item.id}}">
        <text class="invitation-text">{{item.content}}</text>
      </view>
    </scroll-view>
  </view>
</view>

<view class="block-container" wx:if="{{isReferee}}">
  <view class="block-top" bindtap="toggleRefereeInvitationInformForEvent">
    <view class="block-title">
      <text>赛事邀请通知</text>
      <view class="red-dot" wx:if="{{showRefereeInvitationDotForEvent}}">
      </view>
    </view>
    <view class="block-more">
      <image src="/assets/down333.png" mode="" class="{{showRefereeInvitationInformForEvent ? 'icon-rotated' : 'icon'}}"/>
    </view>
  </view>
  <view class="block-main">
    <view class="hint-text">点击邀请可选择接受或拒绝</view>
    <scroll-view scroll-y="true" style="height: {{showRefereeInvitationInformForEvent ? '200px' : '0'}}; visibility: {{showRefereeInvitationInformForEvent ? 'visible' : 'hidden'}};" class="notification-box">
      <view class="invitation-item" wx:if="{{refereeInvitationInformForEvent.length==0}}">
        <text class="invitation-text">您还没有收到任何赛事发出的邀请。</text>
      </view>
      <view class="invitation-item" wx:for="{{refereeInvitationInformForEvent}}" wx:key="index" bind:tap="showRefereeEventInvitationModal" data-id="{{item.id}}">
        <text class="invitation-text">{{item.content}}</text>
      </view>
    </scroll-view>
  </view>
</view> -->